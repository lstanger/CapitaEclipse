@EndUserText.label: 'Abstract Entity Parameter for remove ECC attachment'
@Metadata.allowExtensions: true

define abstract entity ZA_XA_EXTERNAL_KEY{    
     
    ExternalId: abap.char(40); 
    InitialUpload: abap_boolean;
}
